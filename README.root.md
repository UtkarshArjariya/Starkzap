# Dare Board Workspace Notes

This file was the earlier repo-level structure note before everything was consolidated into `dare-board/`.

Current state:
- `dare-board/` is now the single folder containing the app, contracts, docs, tests, and legacy backend reference.
- The live Next.js product runs from the root of `dare-board/`.
- Cairo contracts stay in `dare-board/contracts/`.

Use `dare-board/README.md` as the main current entry point.
